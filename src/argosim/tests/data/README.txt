# This file contain the information used to generate the test case

1. Sky model parameters:
sky_model_params = (
        (256, 256),  # shape_px
        1.0,  # fov
        [0.01, 0.02, 0.03],  # deg_size_list
        [0.4, 0.3, 0.3],  # source_intensity_list
        332,  # seed
    )