[project]
name = "argosim"
readme = "README.md"
requires-python = ">=3.11"
authors = [
    {"name" = "Ezequiel Centofanti", "email" = "ezequiel.centofanti@cea.fr" },
    {"name" = "Emma Ayçoberry", "email" = "Emma.AYCOBERRY@cea.fr" },
]
maintainers = [
    {"name" = "Ezequiel Centofanti", "email" = "ezequiel.centofanti@cea.fr" },
    {"name" = "Emma Ayçoberry", "email" = "Emma.AYCOBERRY@cea.fr" },
]
description = 'Radio interferometric simulations package for the ARGOS radio telescope.'
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
    "scikit-image>=0.22",
    "jax>=0.4",
    "optax>=0.2"
]
version = "1.0.1"

[project.optional-dependencies]
doc = [
    "myst-parser",
    "numpydoc",
    "sphinx",
    "sphinx-book-theme"
]
lint = [
    "black",
    "isort",
]
release = [
    "build",
    "twine",
]
test = [
  "pytest",
  "pytest-cov",
  "pytest-pydocstyle",
]
gui = [
    "pyQt6"
]
# NVIDIA GPU (CUDA 12) support: pulls the matching jaxlib + CUDA plugin as one
# set, so jax / jaxlib / jax-cuda12-plugin versions stay aligned. Requires an
# NVIDIA GPU with a CUDA 12-capable driver. Install with: pip install "argosim[cuda12]"
cuda12 = [
    "jax[cuda12]>=0.4"
]
# Alias
gpu = ["argosim[cuda12]"]
dev = ["argosim[doc,lint,release,test,gui]"]

[tool.pytest.ini_options]
addopts = "--verbose --pydocstyle --cov=argosim"
testpaths = ["src/argosim"]